{
  "name": "puttan-mobile",
  "version": "1.0.0",
  "description": "Puttan - full web app bundled for Android, with native background GPS tracking and push notifications",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:debug": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0",
    "@capacitor/push-notifications": "^8.1.2",
    "@transistorsoft/capacitor-background-geolocation": "^9.3.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
